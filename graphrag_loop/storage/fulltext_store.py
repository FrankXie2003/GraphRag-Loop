"""Elasticsearch 全文检索封装 —— 关键词 / 布尔过滤,与向量检索做混合召回。

# TODO(可选,暂不需要)
当前状态:占位,未实现。
为什么暂不需要:Phase 2 的入口召回(整句向量 + 软链接)已经覆盖大多数场景,
        BM25 关键词检索的边际价值在叙事文本上不大(古典小说词汇变化丰富)。
        如果未来接入"具体术语 / 数字 / 专有名词"密集的文档(法律、医疗、技术手册),
        再考虑加入。

实现要点(若启用):
  - docker-compose.yml 加一个 elasticsearch 服务
  - upsert / search 接口与 vector_store.py 对齐
  - hybrid_recall 加第三路:bm25 召回 → 与向量/软链接结果取并集
"""
