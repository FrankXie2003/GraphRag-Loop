"""IsUse —— 答案有用性评分(Self-RAG 第四个反思 token)。

# TODO(Phase 3:续写任务必需)
当前状态:占位,未实现。
触发条件:**Phase 3 终极测试(红楼梦续写第 51-55 回)** 时启用——
        续写场景下"这段续写好不好、人物性格对不对、伏笔合不合理"恰是 IsUse 的评价对象。

设计要点:
  - 用 reflection.tokens.IsUse 枚举(1-5 分,已定义)
  - 输入:query + 完整答案 + 证据子图(可选 + 续写专属:前文情节链)
  - 输出:1-5 整数评分 + 评分理由
  - 走 LLM 调用(stage='L3_isuse',后续可加进 model_routing)

为什么 Phase 2 没启用:Retrieve/IsRel/IsSup 三个 token 都驱动检索流程(走不走图、剪不剪枝、
        回不回扩),IsUse 只对最终答案打有用性分,不改变流程。简单 QA 场景作用有限。
        续写场景才真正需要它做"质量门" —— 与 CoVe 自我核验配合用。

实现参考:Self-RAG 原文 IsUse 是 5 分类,我们可以更激进:1-3 分 → 触发重写循环。
"""
